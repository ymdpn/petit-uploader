<!DOCTYPE html>
<html lang="ja">
<head>
  <%- include('partials/head', { title: 'ダッシュボード' }) %>
  <script src="https://unpkg.com/dropzone@5/dist/min/dropzone.min.js"></script>
  <link rel="stylesheet" href="https://unpkg.com/dropzone@5/dist/min/dropzone.min.css" type="text/css" />
</head>
<body class="bg-gray-100">
  <div class="container mx-auto p-4">
    <%- include('partials/header', { loginId: loginId }) %>

    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
      <h2 class="text-2xl font-bold mb-4">ファイルアップロード</h2>
      <%- include('partials/uploadForm') %>
    </div>

    <div class="bg-white shadow-md rounded px-8 pt-6 pb-8">
      <h2 class="text-2xl font-bold mb-4">アップロードされたファイル</h2>
      <% if (files.length === 0) { %>
        <p>アップロードされたファイルはありません。</p>
      <% } else { %>
        <ul class="divide-y divide-gray-200">
          <% files.forEach(function(file) { %>
            <%- include('partials/fileItem', { file: file }) %>
          <% }); %>
        </ul>
      <% } %>
    </div>
  </div>

  <%- include('partials/dropzoneScript') %>
</body>
</html>